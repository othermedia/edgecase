Edgecase
========

Elements that scale with the browser window size.

    var boxed = new Edgecase('#image', {
        aspectRatio: 16 / 9,
        container:   '#box'
    });
    
    boxed.setup();
