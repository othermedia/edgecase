---
source_directory:     source
build_directory:      build
layout:               together

builds:
  src:
    packer:           false
    suffix:           false
  min:
    shrink_vars:      true
    private:          false

packages:
  edgecase:
    files:
      - edgecase
    meta:
      requires:
        - JS.Class
        - Ojay
      provides:
        - Edgecase
